Mixed (Multi-level) Zelig Models
================================
This package contains several simple multi-level Zelig models.  It leverages `arm::sim`.  The dev branch relies on `merTools` instead.
