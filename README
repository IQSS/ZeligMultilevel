Mixed (Multi-level) Zelig Models
================================
This package contains several simple multi-level Zelig models.
Currently, it seems that several things are implemented incorrectly,
though the code is error-free
